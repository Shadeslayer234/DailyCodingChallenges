function takeTwoInputsDivideAndRoundDown(){
  const firstNum = Number(prompt('Enter the first value'));
  const secondNum = Number(prompt('Enter the second value'));

   console.log(floor(firstNum / secondNum))
}

const check = takeTwoInputsDivideAndRoundDown();
